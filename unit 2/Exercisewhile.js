var num=Number(prompt("enter the number for print even till number "));
	var count=0;
	Number(count );
	while (count<=num)
	{
		console.log(count);
		count+=2;
	}

var num2=Number(prompt("enter the number for print multiple of 4 till number "));	
var count=1;
while(count<=num2)
{
	if (count%4==0)
	{
		console.log(count);
	}
	count++;
}